({
    leftClick : function(component, event, helper) {
        let component = leftCLick(event);
        
    }
})

({
    rightClick : function(component, event, helper) {
        let component = rightClick(event);

    }
})